
#### 1、分块
分块逻辑：利用 `|` 字符识别表格边界，在确保表格不被切断且段落语义完整的前提下，将长文本智能切割为指定大小的文本块（普通文本按行切割，一个块中会包含多行）。

#### 2、向量化
利用BGE-M3模型将文本块转化为传统的密集向量和稀疏向量。
密集向量即将一段文本转化成一串长长的浮点数（例如 1024 维）。它捕捉的是语义（意思） 。
稀疏向量给每个特定的词分配一个权重，对精确术语、专有名词和罕见缩写非常敏感。
输入
```python
chunks = [
    "eVTOL在城市空域的路径规划算法研究", 
    "无人机调度约束：最大起飞重量与空域划设"
]
```
输出
密集向量与稀疏向量。

#### 3、创建数据库
连接数据库
```python
connections.connect(uri="./milvus.db")
```

定义数据库表结构
```python
fields = [
    FieldSchema(name="pk", dtype=DataType.VARCHAR, is_primary=True, auto_id=True, max_length=10),
    FieldSchema(name="text", dtype=DataType.VARCHAR, max_length=512),
    FieldSchema(name="sparse_vector", dtype=DataType.SPARSE_FLOAT_VECTOR),
    FieldSchema(name="dense_vector", dtype=DataType.FLOAT_VECTOR, dim=dense_dim),
]
schema = CollectionSchema(fields)
```
最终得到的数据库效果

| pk  | text (原文内容)     | sparse_vector           | dense_vector               |
| --- | --------------- | ----------------------- | -------------------------- |
| `1` | “eVTOL在城市空域的路径” | `{eVTOL: 0.9, 路径: 0.7}` | `[0.12, -0.05, 0.88, ...]` |
| `2` | “无人机冲突避撞算法约束”   | `{避撞: 0.8, 约束: 0.6}`    | `[0.45, 0.23, -0.11, ...]` |
于数据库中建表
```python
col = Collection(col_name, schema, consistency_level="Strong")
```

#### 4、创建索引
LlamaIndex框架中使用create_index函数解决即可
例：
稀疏索引：当你搜索“eVTOL”时，CPU 直接根据索引号，瞬间跳到 `doc_1`, `doc_58` 等其存在的文档的位置。
密集索引：根据 `AUTOINDEX`算法，其在计算机中输出的是一个近邻图。搜索时，计算机会像在地图上导航一样，从顶层快速跳跃到与你查询坐标最接近的区域。


#### 5、使用混合检索并给检索结果添加序号
```python
# 混合检索
def hybrid_search(
    col,
    query_dense_embedding,
    query_sparse_embedding,
    sparse_weight=1.0,
    dense_weight=1.0,
    limit=10,
):
    dense_search_params = {"metric_type": "IP", "params": {}}
    dense_req = AnnSearchRequest(
        [query_dense_embedding], "dense_vector", dense_search_params, limit=limit
    )
    sparse_search_params = {"metric_type": "IP", "params": {}}
    sparse_req = AnnSearchRequest(
        [query_sparse_embedding], "sparse_vector", sparse_search_params, limit=limit
    )
    rerank = WeightedRanker(sparse_weight, dense_weight)
    res = col.hybrid_search(
        [sparse_req, dense_req], rerank=rerank, limit=limit, output_fields=["text"]
    )[0]
    return [hit.get("text") for hit in res]
```

- **输入参数**：
    -  `col`：向量库中的数据表。
    - `query_dense_embedding`：查询词的 1024 维语义向量 。
    - `query_sparse_embedding`：查询词的关键词特征向量 。
    - `sparse_weight=1.0`, `dense_weight=1.0`：默认两路搜索同等重要 。
- **计算机内部输出过程**：
    1. Dense 引擎返回 10 条语义最相关的论文片段 。
    2. Sparse 引擎返回 10 条包含“4D”、“eVTOL”关键词最多的片段 。
    3. Rerank 引擎对这 20 个候选结果进行综合评分，去重并重新排序 。
- **函数最终返回**：
    - 返回一个包含 `limit` 条（默认 10 条）最相关论文原文的文本列表。

使用示例：
```python
query = '什么是以人为本的座舱'
query_embeddings = ef([query])
hybrid_results = hybrid_search(
    col,
    query_embeddings["dense"][0],
    query_embeddings["sparse"]._getrow(0),
    sparse_weight=0.7,
    dense_weight=1.0,
)
print(hybrid_results[:3])
```

`hybrid_results` 检索出来后只是一个纯文本列表。还需给每个片段打上编号（如 `[1]`, `[2]`）。
- **输入**：`['eVTOL调度约束A...', '路径规划算法B...']`
- **输出**：
    > [1] eVTOL调度约束A...
    > [2] 路径规划算法B...

作用：约束大模型的“幻觉”，通过这种格式化，你在后续的 Prompt 中可以明确要求 Agent：“请根据参考内容回答，并标注引用编号”。**实际效果**：当 Agent 生成回答时，它会写道：“根据文献 [1] 所示，eVTOL 的最大起飞重量限制了其调度范围...” 。这种带引用的回答比一段没有任何出处的描述更具有科研严谨性。

#### 6、封装成RAG接口

```python
def rag(query):
    formatted_references=format_list_with_markers(retrieval(query))
    #使用混合检索并添加序号
    
    prompt = f"""
    你是一个智能助手，负责根据用户的问题和提供的参考内容生成回答。请严格按照以下要求生成回答：
    1. 回答必须基于提供的参考内容。
    2. 在回答中，每一块内容都必须标注引用的来源，格式为：[引用编号]。例如：[1] 表示引用自第1条参考内容。
    3. 如果没有参考内容，请明确说明。如果没有参考知识，根据你自己的知识进行回答
    参考内容：
    {formatted_references}
    用户问题：{query}
    """
    client = OpenAI(api_key="your_openai_api_key", base_url="https://api.openai-proxy.org/v1")
    completion = client.chat.completions.create(
        model="gpt-4o",
        messages=[
            {
                "role": "user",
                "content": prompt
            }
        ]
    )
    return formatted_references,completion.choices[0].message.content
```

封装了之后输入就为用户询问的问题，输出为根据RAG功能得到的回答。

还可实现其他功能如网络检索
只需在封装的RAG中加入 
web_reference=web_retrieval(query) 
要另外实现
这段通过询问得到的网络检索结果，然后和上面的打编号一样放到prompt中即可。
![[Pasted image 20260411220713.png]]

至此，通过分块、向量化、创建数据库、创建索引、使用混合检索并给检索结果添加序号、封装RAG接口，便实现了从基础的大模型API调用到实现RAG检索或者网络检索的全过程。下面是在RAG（或网络检索）的基础上加上agent。

#### Agent
理论中的agent被拆解为规划、工具、记忆、执行四个核心模块。
本项目的核心是**基于规划 - 工具  - 记忆 - 执行 - 反思**的闭环智能体，结合本地 RAG 与网络搜索，实现汽车销售场景的自主问答。
- **规划模块（Planning）**：负责理解意图、选择工具、拆解任务
- **工具模块（Tools）**：有RAG检索与网络检索（前面已设计）
- **记忆模块（Memory）**：存储每轮提问与检索结果，保证上下文连贯
- **执行模块（Execution）**：负责调用 RAG 检索、网络搜索等工具
- **总结模块（Summarization）**：负责整合记忆内容，生成最终销售话术


##### 在规划模块内部
![[Pasted image 20260414232058.png]]
在调用模型提问时，给了强力的限制条件“只需要输出JSON的部分，前后不要输出任何信息”，同时配合上“`response_format`”模型参数限制，实现其输出固定为JSON形式。

其中prompt设计如下：
```python
    prompt='''
你是一个专业的汽车销售助手的规划模块。你的任务是：
1. 分析用户的查询:{0}
2. 基于已有的信息，决定使用哪个工具来查询以获得更多需要的信息（本地文档搜索或网络搜索）
3. 将用户的原始查询拆解或延伸为1-2个相关问题，以获取更全面的信息

## 可用工具
1. **本地文档搜索**：搜索本地星辰电动ES9的文档，包含以下章节：
   - 产品概述
   - 设计理念
   - 技术规格
   - 驱动系统
   - 电池与充电
   - 智能座舱
   - 智能驾驶
   - 安全系统
   - 车身结构
   - 舒适性与便利性
   - 用户评价
   - 竞品对比
   - 常见问题
   - 联系方式

1. **网络搜索**：在互联网上搜索相关信息

## 工具选择规则
- 当查询明确涉及星辰电动ES9的具体信息、参数、功能或服务时，优先使用**本地文档搜索**
- 当查询涉及以下情况时，使用**网络搜索**：
  - 与其他品牌车型的详细对比
  - 最新市场动态或新闻
  - 非官方的用户体验或评测
  - 星辰电动ES9文档中可能没有的信息
  - 需要实时数据（如当前市场价格波动等）
  
## prompt延伸的规则
- 本地检索的查询扩展侧重于产品信息的深度查询
- 网络检索的查询扩展侧重于本地无法检索到的信息

## 输出格式
你的输出应该是一个JSON格式的列表，每个项目包含：
1. `action_name`：工具名称（"本地文档搜索"或"网络搜索"）
2. `prompts`：问题列表，第一个是原始查询，后面是拆解或延伸的问题
[
  {{
    "action_name": "工具名称",
    "prompts": [
      "原始查询",
      "拆解/延伸问题1",
      "拆解/延伸问题2",
      "拆解/延伸问题3"
    ]
  }}
] 
 
## 示例  
### 示例1：关于车辆规格的查询
用户：星辰电动ES9的续航里程是多少？ 
输出：
[
  {{
    "action_name": "本地文档搜索",
    "prompts": [
      "星辰电动ES9的续航里程是多少？",
      "星辰电动ES9的电池容量是多少？",
      "星辰电动ES9不同版本的续航里程有何区别？"
    ]
  }}
]
    
### 示例2：关于市场比较的查询
用户：星辰电动ES9和特斯拉Model Y相比怎么样？ 
输出：
[
  {{
    "action_name": "本地文档搜索",
    "prompts": [
      "星辰电动ES9的主要优势和特点是什么？",
      "星辰电动ES9的技术规格和配置有哪些？"
    ]
  }},
  {{
    "action_name": "网络搜索",
    "prompts": [
      "特斯拉Model Y主要优势和特点？",
      "特斯拉Model Y最新规格和价格",
      "特斯拉Model Y技术规格和配置有哪些"
    ]
  }}
]
   
### 示例3：关于日常问题
用户：你好
这种情况下都不需要调用，则输出为None
只需要输出JSON的部分，前后不要输出任何信息
'''.format(query)
```
规划agent得到的最终输出
![[Pasted image 20260414224240.png]]

##### 在工具模块和记忆模块内部
工具模块就是基于规划模块的输出调用前面设计的RAG检索和网络检索函数即可。

记忆模块可用简单的python列表来实现
```python
memory_global = [] # 全局列表 
memory_new = [] # 每次执行结果 
memory_global.extend(memory_new) # 追加历史
```
工具模块调用工具的结果也要记得存入到全局记忆中


##### 在总结模块内部
将执行模块检索到的内容添加到用户prompt中，再调用大模型API接口
```python
    final_prompt=f'''
        你是一个星辰电动ES9的智能销售助手，负责根据用户的问题和提供的参考内容生成回答。请严格按照以下要求生成回答：
        基于提供的参考内容进行回答，如果原文没有参考内容,根据你自己的知识进行回答
        你需要用有打动力的销售的语言进行输出，突出星辰电动的优势
        参考内容：
        {memory_global}
        用户问题：{user_query}
    '''
```

##### 最终
在利用工具和记忆实现了：规划、执行、总结三步之后
最终封装成agent接口就如下：
```python
def agent(user_query):
    #记忆模块
    memory_global=[]
    #获得执行清单
    actions=agent_plan(user_query)
    if actions:
        #进行任务执行
        memory_new=process_actions(actions)
        memory_global.extend(memory_new[1:])
    final_answer(memory_global,user_query)
user_query='比较一下和华为汽车的优劣势'
print(agent(user_query))
```

